datasource db {
  provider = "mysql"
  url      = "mysql://root:password@db:3306/itm-db"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String
  password String
}

generator client {
  provider = "prisma-client-js"
}